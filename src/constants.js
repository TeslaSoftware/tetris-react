/** Cell that is filled with tetromino that is fixed and cannot be moved. */
export const CELL_FILLED = 'CELL_FILLED';

/** Cell that is either empty or has tetromino in motion. */
export const CELL_CLEAR = 'CELL_CLEAR';

/** Width of the stage (number of rows). */
export const STAGE_WIDTH = 12;

/** Height of the stage (number of columns). */
export const STAGE_HEIGHT = 20;